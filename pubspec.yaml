name: new_walkthrough
description: "A new Flutter package which helps developers in creating animated walkthrough of their app."
version: 0.0.3
homepage: https://github.com/prajapatihet/Flutter-Walkthrough

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

